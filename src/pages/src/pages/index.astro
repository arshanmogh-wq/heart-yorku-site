---
const title = "HEART — Health Equity Advocacy Response Team";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>

    <style>
      :root {
        --primary: #0f172a;
        --accent: #2563eb;
        --muted: #475569;
        --bg: #f8fafc;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, sans-serif;
        color: var(--primary);
        background: var(--bg);
        line-height: 1.6;
      }

      .container {
        max-width: 1100px;
        margin: 0 auto;
        padding: 4rem 1.5rem;
      }

      h1,
      h2,
      h3 {
        line-height: 1.2;
      }

      h1 {
        font-size: 3rem;
        margin-bottom: 0.5rem;
      }

      h2 {
        font-size: 2rem;
        margin-bottom: 1rem;
      }

      p {
        color: var(--muted);
        max-width: 65ch;
      }

      /* Hero */
      .hero {
        background: white;
        border-bottom: 1px solid #e5e7eb;
      }

      .hero p {
        font-size: 1.1rem;
        margin-top: 1rem;
      }

      .buttons {
        margin-top: 2rem;
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
      }

      .btn {
        padding: 0.75rem 1.25rem;
        border-radius: 6px;
        font-weight: 500;
        text-decoration: none;
        border: 1px solid transparent;
      }

      .btn.primary {
        background: var(--accent);
        color: white;
      }

      .btn.secondary {
        border-color: var(--accent);
        color: var(--accent);
        background: transparent;
      }

      /* Sections */
      section {
        background: white;
        margin-top: 3rem;
        border-radius: 12px;
        padding: 3rem;
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
      }

      .card {
        border: 1px solid #e5e7eb;
        border-radius: 10px;
        padding: 2rem;
      }

      .card h3 {
        margin-top: 0;
      }

      /* Footer */
      footer {
        text-align: center;
        padding: 2rem 1rem;
        color: #64748b;
        font-size: 0.9rem;
      }
    </style>
  </head>

  <body>
    <!-- Hero -->
    <header class="hero">
      <div class="container">
        <h1>HEART</h1>
        <h3>Health Equity Advocacy Response Team</h3>

        <p>
          HEART is a student-led organization dedicated to advancing health
          equity by addressing systemic, social, and economic barriers to health.
        </p>

        <div class="buttons">
          <a href="/join" class="btn primary">Join HEART</a>
          <a href="/about" class="btn secondary">Learn More</a>
        </div>
      </div>
    </header>

    <!-- Mission -->
    <section class="container">
      <h2>Our Mission</h2>
      <p>
        To advocate, educate, and take action in response to health inequities by
        empowering communities, supporting evidence-based initiatives, and
        promoting inclusive healthcare systems.
      </p>
    </section>

    <!-- What We Do -->
    <section class="container">
      <h2>What We Do</h2>

      <div class="grid">
        <div class="card">
          <h3>Advocacy</h3>
          <p>
            We raise awareness about health disparities and advocate for
            equitable health policies that support underserved communities.
          </p>
        </div>

        <div class="card">
          <h3>Community Engagement</h3>
          <p>
            We collaborate with local organizations through volunteering,
            outreach initiatives, and community partnerships.
          </p>
        </div>

        <div class="card">
          <h3>Education</h3>
          <p>
            We deliver accessible, evidence-based health education and work to
            combat misinformation through clear communication.
          </p>
        </div>
      </div>
    </section>

    <!-- Get Involved -->
    <section class="container">
      <h2>Get Involved</h2>
      <p>
        HEART welcomes students from all disciplines who are passionate about
        health equity, social justice, and community service.
      </p>

      <div class="buttons">
        <a href="/join" class="btn primary">Become a Member</a>
        <a href="/contact" class="btn secondary">Contact Us</a>
      </div>
    </section>

    <footer>
      © {new Date().getFullYear()} HEART — Health Equity Advocacy Response Team
    </footer>
  </body>
</html>
